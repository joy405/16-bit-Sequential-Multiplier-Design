# 16-bit-Sequential-Multiplier-Design
This project implements a 16-bit Sequential (Shift-and-Add) Multiplier using Verilog HDL. The design is partitioned into a Datapath Unit (DP) and a Control Unit (CU) based on a Finite State Machine (FSM) to ensure efficient hardware utilization and timing accuracy.
